{"steps":[{"title":"Getting Started","body":"<p>This guide will walk you through writing your first few lines of code with voice. We’ll go through a few different examples that cover adding, navigating, and editing code.</p><p>To start, click Next below.</p>","skipEditorFocus":true},{"title":"Setup","body":"<p>Serenade will send voice commands to whichever application you have in the foreground. You can see which application Serenade is controlling via the icon at the bottom-right.</p><p>To get started, open VS Code, Atom, or JetBrains, then click next.</p>","nextWhenEditorFocused":true},{"title":"Setup","body":"<p>Before we start the tutorial, let's make sure we're using an empty file. In your editor (without using voice), create a new file and save it as <code>serenade.java</code>.</p>","nextWhenEditorFilename":true},{"title":"Alternatives","body":"<p>As you speak, you’ll see a numbered list of possible commands appear below. The first command runs automatically when you’ve finished speaking. To select a different command, just say its number, like “two” or “three”, and Serenade will run that command instead.</p><p>Let's get started! First, toggle the listening switch above to enable Serenade. Then, say:</p>","transcript":"next","textOnly":true},{"title":"Understanding commands","body":"<p>Most Serenade commands consist of an <i>action</i> followed by a <i>selector</i>.</p><p>An action describes what you're trying to do, like “go to”, “add”, or “insert”. A selector describes the object you're trying to change, like a “line”, “word”, “function”, or “class”.</p><p>We'll see examples throughout this tutorial! To continue, say:</p>","transcript":"next","textOnly":true},{"title":"Inserting code","body":"<p>We'll start with one of the most common actions, “insert”. The “insert” action will insert text directly at your cursor. Try it out by saying:","source":"","cursor":0,"transcript":"insert import random","resetSource":"","matches":["insert <code>import random</code>"]},{"title":"Undo","body":"<p>You should now see some code in your editor! To undo any command, you can simply say “undo”. Try it out:</p>","source":"import random","cursor":13,"transcript":"undo"},{"title":"Writing code","body":"<p>In this first example, we’re going to write a factorial method from scratch.</p><p>To continue, say:</p>","source":"","cursor":0,"transcript":"next","resetSource":"","textOnly":true},{"title":"Writing a factorial method","body":"<p>Take a look at your editor—that's the code we'll be writing with voice.</p><p>When you're ready to start, say:</p>","source":"class App {\n  public int factorial(int number) {\n    if (number == 0) {\n      return 1;\n    }\n    else {\n      return number * factorial(number - 1);\n    }\n  }\n}\n","cursor":0,"transcript":"next","resetSource":"class App {\n  public int factorial(int number) {\n    if (number == 0) {\n      return 1;\n    }\n    else {\n      return number * factorial(number - 1);\n    }\n  }\n}\n","textOnly":true},{"title":"Adding a class","body":"<p>While “insert” inserts text at your cursor, the “add” action is used to create new statements and constructs like functions or classes, without needing to dictate any boilerplate.</p><p>Let's give it a try! Create a new class called <code>App</code> by saying:</p>","source":"","cursor":0,"transcript":"add public class app","resetSource":"","matches":["add class <code>public class App {\n}</code>"]},{"title":"Adding a method","body":"<p>Next, create a method called <code>factorial</code> by saying:</p>","source":"public class App {\n}\n","cursor":16,"transcript":"add method public int factorial","matches":["add <code>public int factorial() {\n}</code>"]},{"title":"Adding a parameter","body":"<p>That method will need a parameter, so add one with:</p>","source":"public class App {\n  public int factorial() {\n  }\n}\n","cursor":41,"transcript":"add parameter int number","matches":["add parameter <code>int number</code>"]},{"title":"Adding a condition","body":"<p>Next, let's add a condition to check if the <code>number</code> parameter we just added equals zero. Say:</p>","source":"public class App {\n  public int factorial(int number) {\n  }\n}\n","cursor":52,"transcript":"add if number equals zero","matches":["add <code>if (number == 0) {\n}</code>"]},{"title":"Adding a statement","body":"<p>Now, add a return statement by saying:</p>","source":"public class App {\n  public int factorial(int number) {\n    if (number == 0) {\n    }\n  }\n}\n","cursor":75,"transcript":"add return 1","matches":["add <code>return 1;</code>"]},{"title":"Adding a branch","body":"<p>Great! Now our if branch is done. Let's create an else branch by saying:</p>","source":"public class App {\n  public int factorial(int number) {\n    if (number == 0) {\n      return 1;\n    }\n  }\n}\n","cursor":93,"transcript":"add else","matches":["add <code>else {\n}</code>"]},{"title":"Finishing the method","body":"<p>So far, we've been using short commands, but you can also dictate longer commands with Serenade.</p><p>Let's try finishing up the factorial function with a single voice command. Say:</p>","source":"public class App {\n  public int factorial(int number) {\n    if (number == 0) {\n      return 1;\n    } else {\n    }\n  }\n}\n","cursor":107,"transcript":"add return number times factorial of number minus one","matches":["add <code>return number * factorial(number - 1);</code>"]},{"title":"Editing code","body":"<p>Congrats, you just wrote your first code with voice!</p><p>Now that we've covered some commands for adding new code, let's try editing some existing code.</p><p>To continue, say:</p>","source":"public class App {\n  public int factorial(int number) {\n    if (number == 0) {\n      return 1;\n    } else {\n      return number * factorial(number - 1);\n    }\n  }\n}\n","cursor":151,"transcript":"next","textOnly":true},{"title":"Editing code","body":"<p>Your editor should now have a simple class as a starting point—we're going to make some edits to this code.</p><p>When you're ready to start, say:</p>","source":"import bank.Account;\nimport java.util.List;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount, int date) {\n    this.balance += amount;\n  }\n}\n","cursor":0,"transcript":"next","resetSource":"import bank.Account;\nimport java.util.List;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount, int date) {\n    this.balance += amount;\n  }\n}\n","textOnly":true},{"title":"Navigating code","body":"<p>Let's start by moving our cursor to line 2. Just like before, we can use the “go to” action and the “line” selector.</p><p>Say:</p>","transcript":"go to line 2"},{"title":"Deleting code","body":"<p>It looks like this import isn't being used, so let's remove it with the “delete” action.</p><p>Say:</p>","source":"import bank.Account;\nimport java.util.List;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount, int date) {\n    this.balance += amount;\n  }\n}\n","cursor":21,"transcript":"delete line"},{"title":"Default action","body":"<p>Since the “go to” action is so common, if you just say a selector, then “go to” will be the default action.</p><p>Try it out by saying:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount, int date) {\n    this.balance += amount;\n  }\n}\n","cursor":21,"transcript":"second method"},{"title":"Deleting","body":"<p>It looks like the second parameter of this method isn't being used either.</p><p>Delete it by saying:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount, int date) {\n    this.balance += amount;\n  }\n}\n","cursor":156,"transcript":"delete second parameter"},{"title":"Copying","body":"<p>Now, let's try the “copy” action.</p><p>Say:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n}\n","cursor":179,"transcript":"copy method"},{"title":"Chaining","body":"<p>You can chain commands together to go even faster. To paste the method you copied to the end of the class, you can chain a “go to” command and a “paste” command.</p><p>Say:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n}\n","cursor":179,"transcript":"end of method paste","matches":["paste"]},{"title":"Go to text","body":"<p>You can also use “go to” to jump to specific text.</p><p>To jump to the nearest instance of <code>deposit</code>, say:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n}\n","cursor":275,"transcript":"go to deposit","matches":["go to <code>deposit</code>"]},{"title":"Changing text","body":"<p>To change existing text, you can use the “change” action with a selector. Since your cursor is on the word <code>deposit</code>, you can change the name of this method by saying:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n}\n","cursor":222,"transcript":"change word to withdraw","matches":["change word to <code>withdraw</code>"]},{"title":"Changing text","body":"<p>You can also directly specify the text you want to change, including symbols.</p><p>For instance, try saying:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void withdraw(int amount) {\n    this.balance += amount;\n  }\n}\n","cursor":230,"transcript":"change next plus to minus","matches":["change next <code>+</code> to <code>-</code>"]},{"title":"Text formatting","body":"<p>When adding or changing text, you can also specify how to format the text with “underscores”, “camel case”, “pascal case”, and so on.</p><p>First, let's go to the start of the class.</p><p>Say:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void withdraw(int amount) {\n    this.balance -= amount;\n  }\n}\n","cursor":263,"transcript":"start of class"},{"title":"Text formatting","body":"<p>Now, add some formatted text by saying:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void withdraw(int amount) {\n    this.balance -= amount;\n  }\n}\n","cursor":22,"transcript":"add int property all caps min balance equals negative 100","matches":["add property <code>int MIN_BALANCE = -100;</code>"]},{"title":"Adding a parent class","body":"<p>Finally, let's have this class inherit from a parent class called <code>Account</code>. The “add” action will add code to the logical spot closest to the cursor, so from here, you can just say:</p>","source":"import bank.Account;\n\nclass CheckingAccount {\n  int MIN_BALANCE = -100;\n\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void withdraw(int amount) {\n    this.balance -= amount;\n  }\n}\n","cursor":70,"transcript":"add extends account","matches":["add extends <code>Account</code>"]},{"title":"Style","body":"<p>To style your source code, say:</p>","source":"import bank.Account;\n\nclass CheckingAccount extends Account {\n  int MIN_BALANCE = -100;\n\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n  void withdraw(int amount) {\n    this.balance -= amount;\n  }\n}\n","cursor":59,"transcript":"style file"},{"title":"Try it out","body":"<p>Nice job! Now you know some commands for adding, navigating, and editing code.</p><p>For this last example, you'll be writing some code on your own. Don't fret—if you get stuck, just click “show hint”.</p><p>When you're ready to start, say:</p>","source":"import bank.Account;\n\nclass CheckingAccount extends Account {\n\n  int MIN_BALANCE = -100;\n\n  public CheckingAccount(String name, int balance) {\n    this.name = name;\n    this.balance = balance;\n  }\n\n  void deposit(int amount) {\n    this.balance += amount;\n  }\n\n  void withdraw(int amount) {\n    this.balance -= amount;\n  }\n}\n","cursor":59,"transcript":"next","textOnly":true},{"title":"Adding a class","body":"<p>We're going to write a method that takes a name as a parameter, then prints out a greeting.</p><p>First, create a class called <code>App</code>.</p>","source":"","cursor":0,"transcript":"add public class app","resetSource":"","resetCursor":0,"matches":["add class <code>public class App {\n}</code>"],"hideAnswer":true},{"title":"Adding a method","body":"<p>Nice job! Next, create a method called <code>sayHello</code></p>","source":"public class App {\n}\n","cursor":16,"transcript":"add method say hello","matches":["add <code>public void sayHello() {\n}</code>"],"hideAnswer":true},{"title":"Adding a parameter","body":"<p>Now, add a <code>String</code> parameter to your method called <code>name</code></p>","source":"public class App {\n  public void sayHello() {\n  }\n}\n","cursor":41,"transcript":"add parameter string name","matches":["add parameter <code>String name</code>"],"hideAnswer":true},{"title":"Adding a statement","body":"<p>Let's start writing the body of our method. First, add the line:</p><p><code>String message = \"Hello\";</code></p>","source":"public class App {\n  public void sayHello(String name) {\n  }\n}\n","cursor":53,"transcript":"add string message equals string hello","matches":["add <code>String message = \"hello\";</code>"],"hideAnswer":true},{"title":"Inserting text","body":"<p>Next, insert the following on the current line:</p><p><code>+ name</code></p>","source":"public class App {\n  public void sayHello(String name) {\n    String message = \"hello\";\n  }\n}\n","cursor":85,"transcript":"insert plus name","matches":["insert <code> + name</code>"],"hideAnswer":true},{"title":"Adding a call","body":"<p>Great! Now we can print out the <code>message</code> variable. Add a line that looks like:</p><p><code>System.out.println(message);</code></p>","source":"public class App {\n  public void sayHello(String name) {\n    String message = \"hello\" + name;\n  }\n}\n","cursor":92,"transcript":"add print message","matches":["add <code>System.out.println(message);</code>"],"hideAnswer":true},{"title":"Navigating code","body":"<p>Let's make one last edit to our function. First, move your cursor to line 2.</p>","source":"public class App {\n  public void sayHello(String name) {\n    String message = \"hello\" + name;\n    System.out.println(message);\n  }\n}\n","cursor":124,"transcript":"line 2","hideAnswer":true},{"title":"Changing text","body":"<p>Finally, let's change the name of the method from <code>sayHello</code> to <code>printGreeting</code>","source":"public class App {\n  public void sayHello(String name) {\n    String message = \"hello\" + name;\n    System.out.println(message);\n  }\n}\n","cursor":19,"transcript":"change say hello to print greeting","matches":["change <code>sayHello</code> to <code>printGreeting</code>"],"hideAnswer":true},{"title":"Congrats!","body":"<p>Awesome! We've covered the basics of adding, navigating, and editing code. Here are a few more tips for getting started with Serenade.</p>","source":"public class App {\n  public void printGreeting(String name) {\n    String message = \"hello\" + name;\n    System.out.println(message);\n  }\n}\n","cursor":46,"skipEditorFocus":true},{"title":"Community","body":"<p>The Serenade Community is a Discord server where you can get help directly from the Serenade team and meet other people using Serenade.</p><a href=\"https://serenade.ai/community\" target=\"_blank\" class=\"primary-button block text-center\">Join the community</a>","skipEditorFocus":true},{"title":"Documentation","body":"<p>To learn more about all the voice commands supported by Serenade, check out the Serenade documentation.</p><a href=\"https://serenade.ai/docs\" target=\"_blank\" class=\"primary-button block text-center\">Open documentation</a>","skipEditorFocus":true},{"title":"Try it out!","body":"<p>That's it for our tutorial!</p><p>Don't hesitate to reach out in the community if you run into any issues.</p>","skipEditorFocus":true,"last":true}],"filename":"java-basics.java"}